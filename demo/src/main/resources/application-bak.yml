spring:
  datasource:
    url: jdbc:mysql://localhost:3306/calcite_conf
    username: root
    password: Qq111111
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource  # 指定使用 HikariDataSource

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.example.demo.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

calcite:
  datasource-file: "/root/tmpfile/model.json"